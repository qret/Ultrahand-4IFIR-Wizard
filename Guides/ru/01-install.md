# Установка

<!-- nav:begin -->
[Оглавление](README.md) · [Первый запуск →](02-first-run.md)  
**Русский** · [English](../en/01-install.md)
<!-- nav:end -->

Тюнер ставится **поверх уже установленной 4IFIR**. Саму прошивку он не трогает:
загрузчик, `loader.kip`, оверлей 4IFIR, системные модули остаются как есть.

Меняются только две вещи: движок оверлеев и пакет настройки.

## Шаг 1. Удалить старое

Удалите с карты памяти:

```
config\uberhand\
switch\.packages\4IFIR Wizard\
switch\.packages\config.ini
```

Это остатки старого движка и старого визарда. Новый пакет их не понимает, а вместе
они будут мешать друг другу.

> [!IMPORTANT]
> **Не удаляйте `atmosphere\kips\kip-json\`.** Это ваши сохранённые профили настроек,
> а не служебные файлы. Тюнер умеет их читать — [импорт старых профилей](10-profiles.md).

## Шаг 2. Скопировать архив

Распакуйте содержимое архива **в корень карты памяти**, с заменой.

Старый `ovlmenu.ovl` заменится новым сам — удалять его отдельно не нужно.

> [!IMPORTANT]
> **Если Ultrahand у вас уже стоит — не копируйте два файла:**
>
> - `config\ultrahand\config.ini` — в нём ваша комбинация клавиш, тема, язык;
> - `config\ultrahand\overlays.ini` — в нём порядок оверлеев, избранное, скрытые.
>
> Скопируете — потеряете свои настройки. Всё остальное копируется без опаски.
>
> Ставите Ultrahand впервые — копируйте всё, оба файла нужны.

## Шаг 3. Проверить

Откройте оверлей: **`L` + `R` + `↑`**

Он открывается на **списке оверлеев** — нажмите **вправо**, откроется список
**пакетов**. Там и лежит **4IFIR Wizard**.

Зайдите: сверху `eBAMATIC Stage`, ниже `Advanced`, `Service` и обновление.

Пакета нет — либо архив распакован не в корень карты, либо остались файлы из шага 1.

## Если комбинация не работает

Она задана в `config\ultrahand\config.ini`.

> [!WARNING]
> Комбинация в `config\tesla\config.ini` должна **совпадать** с той, что в
> `config\ultrahand\config.ini`. Если они разойдутся, оверлеи начнут спорить за одно
> нажатие.

## Как вернуть всё к исходному

Чистая установка 4IFIR: удалите с карты **всё, кроме папок `Nintendo` и `emummc`**,
и распакуйте сборку 4IFIR заново.

`Nintendo` — ваши игры и сохранения, `emummc` — виртуальная память консоли.
Их трогать нельзя.

---

<!-- nav:begin -->
[Оглавление](README.md) · [Первый запуск →](02-first-run.md)  
**Русский** · [English](../en/01-install.md)
<!-- nav:end -->
