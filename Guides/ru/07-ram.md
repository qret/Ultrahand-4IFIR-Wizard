# Память

<!-- nav:begin -->
[← Видеоядро и ступени](06-gpu.md) · [Оглавление](README.md) · [Тайминги и тонкая настройка →](08-timings.md)  
**Русский** · [English](../en/07-ram.md)
<!-- nav:end -->

Раздел `Advanced → RAM`. Самый выгодный узел: разгон памяти даёт больше всего
и почти не сказывается на расходе батареи.

И самый капризный: именно здесь чаще всего ловят зависания.

## Порядок настройки

Он не произвольный, и оба донорских руководства, и пошаговый мастер самой прошивки
идут в одном порядке:

1. `Frequency` — частота;
2. `EMC Balance`;
3. `Vddq` и `Vdd2` — напряжения;
4. `EMC DVB Mode`;
5. `EBA-Shift`;
6. `Core Timings` — тайминги.

Тайминги **последними**, а напряжения **до** них: шестой и седьмой тайминги прямо
зависят от `Vdd2`.

## Частота

`Frequency` задаёт частоту памяти. Значения подписаны числом и режимом, для которого
они рассчитаны, — например `1862MHz — 1862 — ECO ST2 eb2`.

1. Поднимите частоту на одну ступень.
2. Перезагрузитесь.
3. Прогоните тест памяти, потом игру.
4. Не завелось или падает — вернитесь.

## EMC Balance

Задаёт, что для прошивки важнее: **отдача или достижимая частота**.

Чем выше значение, тем выше берётся частота — и, что неочевидно, тем **ниже** нужное
напряжение памяти. Чем ниже значение, тем плотнее тайминги и выше отдача на низких
частотах, но высокая частота не возьмётся.

> [!TIP]
> Не завелась частота — закрепите `EMC Balance` вручную **выше** того, что подобрал
> автомат: `3`, затем `4`, затем `5`.
>
> **Не берите `1` и `2`** — это режимы максимальной отдачи на низких частотах, они
> мешают взять высокую. Заводское значение — автоподбор.

Если частота не берётся без видимой причины, у прошивки есть отдельный регулятор
именно для этого случая — `pMeh 1 divMB Supressor` в `Micro-Enhance Logic`.

## Напряжения памяти

- **`Vddq`** — напряжение линий данных памяти;
- **`Vdd2`** — второе питание памяти.

Прошивка выбирает оба сама по связке «частота + `EMC Balance`». Поля задают
переопределение. Поэтому сначала `EMC Balance`, и только потом напряжения руками.

> [!WARNING]
> **Слишком низкий `Vddq` — консоль не загрузится.** Заводское значение 650 мВ.
>
> Донорские руководства дополнительно предупреждают о риске повредить EmuNand.
> Подтверждения этому в самой прошивке мы не нашли, но цена ошибки такова, что
> предупреждение стоит передать как есть.

## EMC DVB Mode

Задаёт напряжение домена SoC, в котором живёт контроллер памяти. **Только Mariko.**

`Eco ST1/ST2/ST3` — ступени автоматического андервольта, третья самая глубокая.
Можно задать и числом.

Отправную точку донорское руководство советует брать из монитора: посмотрите
**напряжение SoC** под нагрузкой и выбирайте близкое значение. Стало нестабильно —
первым делом верните автомат.

## EBA-Shift

Сдвигает режим `EMC Balance` для части производных величин. На практике это регулятор
**восьмого тайминга**: чем меньше разница между `EMC Balance` и `EBA-Shift`, тем выше
восьмой тайминг можно поднять.

> [!IMPORTANT]
> Ось у него **обратная** `EMC Balance`. Два соседних пункта меню тянут в разные
> стороны — это лёгкая ловушка.

Проверять надо и цифрой в тесте, и устойчивостью: при артефактах и зависаниях значение
понижают.

## Optimized Mode (1600 MHz)

Подраздел с тремя полями профиля **4IFIR Optimized**. Это те же `sMeh 16`, `pMeh 20`
и `sMeh 8` из `Micro-Enhance Logic`, только под понятными именами.

Они действуют **только в этом профиле** и не заменяют настройку основной частоты.

> [!WARNING]
> `Efficiency Stages` в этом подразделе — тот самый регулятор, из-за которого
> появляются **полосы на экране в доке**. Если увидели полосы, верните его в ноль.
> Это одно поле с `sMeh 8 E-Boost`, сбрасывать можно любой из двух.

## Тайминги

Восемь пунктов `Core Timings` разобраны отдельно:
[Тайминги и тонкая настройка](08-timings.md).

Коротко: трогать их стоит **последними**, когда частота и напряжения устоялись.

---

<!-- nav:begin -->
[← Видеоядро и ступени](06-gpu.md) · [Оглавление](README.md) · [Тайминги и тонкая настройка →](08-timings.md)  
**Русский** · [English](../en/07-ram.md)
<!-- nav:end -->
